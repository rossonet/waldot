# WaldOT Production Bootstrap Configuration
# This file is loaded on startup to initialize the graph structure

# Example: Create a production monitoring setup
# Uncomment and customize as needed

# Create a generator for temperature monitoring
# g.addV('generator').property('type', 'generator').property('label', 'temp-sensor-1').property('Algorithm', 'sinusoidal').property('Min', '18').property('Max', '26').property('Delay', '5000')

# Create a rule for temperature alerts
# g.addV('rule').property('type', 'rule').property('label', 'temp-alarm').property('Condition', 'temperature > 25.0').property('Action', "log.warn('High temperature: ' + temperature)")

# Create compute manager
# g.addV('compute').property('type', 'compute').property('label', 'main-compute').property('Threads', '4')
